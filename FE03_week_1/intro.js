var user = { name: "sahil", age: 26 };
console.log(user.name);
// Given code won't work when we try to execute, this is where tsx comes into picture 
// we use tsc intro.ts it will create a js file of the .ts file 
