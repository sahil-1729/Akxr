- Before making project, always plan it out 

- For netflixGPT these are things to be planned
# Features 
- Login/sign up
    - Sign up/ Sign in 
    - redirect to Browse page 
- Browse(after authentication)
    - Header
    - Main movie 
        - Trailer in background 
        - Title & Description
        - Movie suggestion
            - MovieLists * N

- NetflixGPT
    - Search bar 
    - Movie suggestion


### When your inputs becomes too big for your app
- it becomes very difficult to manage those inputs 
- also difficult to validate those inputs
- so for this case its useful to make use of third party libraries 
    - e.g formik for forms in react 


### useRef
- it returns a object, which provides the reference to the element we refered
- it is initialized like this 
    - const email = useRef(null)
        - setting the intial default value as null
    - using it at the tag
    - <div ref={email}></div>
    - to access it we use email.current 
        - .current refers to the current referred object or element
- it is used to refer a field or a tag here, like input field 


---

steps to setup redux
- configure react redux store - 2:54:00
- configure slice using createSlice for user 
- add slice to the store
- add the store in app.js 

- now use the store
    - the store to be used
        - whn user signs in 
        - whn user sign up
        - whn user log out
    - instead of utilizing the store by calling it in every fn(like sign in pe, sign out pe, etc)
        - we could make use of firebase ka provided fn - onAuthStateChange 
            - where, whenever there will be sign out or sign in or log out like this event
                - this fn will be called
                    - this is where we could add the redux store ka code, instead of pasting it in every fn


- Always store the constants, in the constants.js file 
    - like the logo url 
    - adding hardcoded values in constants.js file 

- login into tmdb and get the API and API access token 
- read API docs and get the details of the API to be used, for now using "Now playing" api 
- use the API - nowPlaying to get the list of movies and log into console

- fetch the data from API and push it into the movieReducer store

- create custom hooks
    - how i did? by writing the logic in another file and import it in browse.js file

- while building UI
    - build in terms of divide and rule
        - divide each part into smaller manageable parts and focus on each small part 

- show the video details in videoTitle component

- make use of videos API to get the details of the videos and include in videoBackground component

- to dynamically add the trailer id recieved from the tmdb API into the iframe tag in the videoBackground file, 
- refactor code - add custom hook in videoBackground component 
    - this is basically modular coding 
        - i.e breaking into smaller modules 

- remove console logs
- add css to videoBackground file 
- create the moviesList and moviesCard component
- display the images in the moviesList and movieCard component
- add css to movieCard component
- add css to movieList component 
- fix bugs
- build GPT search feature
    - build gpt button, on click will show gpt searhc component 
    - build gpt search page ke components
        - search bar
        - search bar se suggestions
- always create sepearate constants file for text content 
- Never push console.log in prodn 
- multi language feature 

- Memoization
    - in netflix ka home page me, every time we reach that page, we make api call
    - even though we have data required to play the trailer, still we make the api call, which we don't need to make eventually
    - for this we can make use of memoization 
        - starts from ep 16 3:10:00